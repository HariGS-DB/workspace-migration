resources:
  jobs:
    pre_check:
      name: "${var.job_prefix}-pre-check"
      tasks:
        - task_key: pre_check
          notebook_task:
            notebook_path: ../src/pre_check/pre_check.py
